
//Lectura de un archivo de texto 
#include<stdio.h>
#include<string.h>
#include<stdlib.h>

/*
int main()
{
    FILE *ent=fopen("ArchivoPowell.txt", "w"); //fopen -abre archivo 
    fprintf(ent, "HOla"); 

    return 0;
}

int main(int argc, char *argv[]) //escribier
{
    FILE *ENT=fopen(argv[1], "w");
    for
    return 0;
}

//Te pide tu nombre y lo guarda en el archivo de texto 

int main(){
   FILE *sal=fopen("Salida.txt","a");
   char pal[100];
   int s;
   do{
      printf("Como te llamas? ");
      scanf("%s",pal);
      if((s=strcmp(pal,"x"))!=0)
           fprintf(sal,"%s\n",pal);
   }while(s!=0);
   fclose(sal);

   return 0;
}

*/
//Va a leer letra por leta o char por char y lo imprime en la terminal 
int main(int argc, char const *argv[])
{
    int c;
    FILE *ENT=fopen("Salida.txt", "r")
    while(cfgetc(ent, c))
        putchar(c);
    
    return 0;
}
Arch 5[0] --Archivo inicial 
argv [1] --- Santiago  (Comienzas a leer o aescribir , inicializado en la posicion 1 del archivo)
texto.txt [2]--- 


