#include<stdio.h>

int main()
{
    FILE *SAL=fopen("Salida.txt", "w"); //Aqui escribe 

    char pal[100];
    fscanf(ent, "%s.\n")
    prinf("Como te llamas? "); //Rite en el archivo en la pantalla o salida estandar
    scanf("%s", sal, "%S\n", pal);
    fclouse(sal);
    
    return 0;
}
