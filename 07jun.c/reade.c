#include<stdio.h>

int main()
{
    FILE *Ent=fopen("Salida.txt", "r"); //Aqui escribe 
    char pal[100];
    printf("%d", EOF) //EOF End of file con numeros magicos
    //Mientras no encuentre el fin del archivo 
    whiile (){
    printf("%S")
    

    print("%d.\n", fscanf(Ent, "%s%s%s\n", pal, pal2, pal3)); //Tres cadenas que guardo en pal 1, 2 y 3
    

    return 0;
    }
    prinf("%s"); 
    scanf(Ent, "%S\n", pal);
    printf("%S\n", pal);
    
    return 0;
}

//El numero de retorno de la funcion es el numero de cadenas que pudo leer exitosamente
//Ordenar cadenas con buble sort 
